# --- Unreal Engine (plugin build artifacts, host-project leftovers) ---
Plugin/Uplink/Binaries/
Plugin/Uplink/Intermediate/
Binaries/
Intermediate/
Saved/
DerivedDataCache/
.vs/
*.sln
*.suo
*.VC.db
*.VC.opendb

# --- Node bridge ---
bridge/node_modules/
npm-debug.log*

# --- Machine-local config (never commit host-project paths) ---
scripts/hosts.local.json
# Names of other projects this machine works on, for check_repo.ps1 to grep
# for. The list itself must never be committed - it is the thing being kept out.
scripts/private_terms.local.txt

# --- Local working notes (never commit) ---
docs/
notes/
*.notes.md
# Experiments live outside the repo until they have a result worth showing.
benchmark/

# --- OS noise ---
Thumbs.db
Desktop.ini
.DS_Store
